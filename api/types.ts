export type LoginUserData = {
    username: string,
    password: string
}

export type RegisterUserData = {
    username: string,
    email:string,
    password: string
    type:string
}

export type ResponceUserData = {
    name: string,
    email:string,
    password: string
    type:string
}